/**
 * Frontend API Configuration
 * 
 * - If you are hosting the backend on Railway (or another server):
 *   Set API_BASE_URL to your deployed Railway domain:
 *   e.g. window.API_BASE_URL = "https://your-app.up.railway.app";
 * 
 * - If you are hosting both frontend and backend on Vercel:
 *   Leave window.API_BASE_URL as "" (empty string).
 */
window.API_BASE_URL = "";
